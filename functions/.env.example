# Firebase Functions environment variables (dotenv)
# Copy this file to `.env` (and optionally `.env.local`, `.env.qa`, etc.)
# then fill in the real credentials. Never commit the populated files.

# SMTP configuration for email OTP service
SMTP_FROM_EMAIL=cringeebank@gmail.com
SMTP_USER=cringeebank@gmail.com
SMTP_FROM_NAME=CringeBank
SMTP_PASSWORD=YOUR_APP_PASSWORD_HERE
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465

# Optional advanced SMTP controls
# SMTP_APP_PASSWORD=
# SMTP_REQUIRE_TLS=true
# SMTP_DISABLE_TLS_VERIFICATION=false

# Realtime mirror (Azure Service Bus)
SERVICEBUS_CONNECTION_STRING=Endpoint=sb://your-servicebus-namespace.servicebus.windows.net/;SharedAccessKeyName=write-shared-policy;SharedAccessKey=YOUR_KEY
SERVICEBUS_TOPIC_REALTIME_MIRROR=realtime-mirror
SERVICEBUS_SUBSCRIPTION_SQL_WRITER=sql-writer
SERVICEBUS_SUBSCRIPTION_MONITORING=monitoring
# Optional overrides
# SERVICEBUS_DEADLETTER_ALERT_THRESHOLD=10
# SERVICEBUS_PUBLISH_RETRY=5

# SQL Server mirror storage
SQLSERVER_HOST=your-sql-host.database.windows.net
SQLSERVER_PORT=1433
SQLSERVER_DB=CringeRealtime
SQLSERVER_USER=sqlmirror_writer
SQLSERVER_PASS=super-secure-password
# Optional SQL pool tuning
# SQLSERVER_POOL_MAX=20
# SQLSERVER_POOL_MIN=0
# SQLSERVER_POOL_IDLE=30000
# SQLSERVER_ENCRYPT=true
# SQLSERVER_TRUST_CERT=false

# Feature flags
USE_SQL_DM_WRITE_MIRROR=false

# Local worker overrides
# SQL_MIRROR_ENV_FILE=../.env.qa
